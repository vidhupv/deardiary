# deardiary

Voice-first journaling app. Call a number, talk to the agent, the conversation becomes today's entry. Web app surfaces those entries plus a Life OS dashboard.

## Setup

Requires Node 18+.

```bash
npm install
npm run dev
```

Open http://localhost:5173.

## Build

```bash
npm run build
```

Outputs to `dist/`.
